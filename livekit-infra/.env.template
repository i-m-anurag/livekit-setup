# =============================================================================
# LiveKit Infrastructure - Environment Variables
# Copy to .env and fill in before running: docker compose up -d
# =============================================================================

# --- LiveKit API Credentials ---
# MUST match the 'keys:' section in livekit.yaml
# Generate: openssl rand -hex 16  (key)
# Generate: openssl rand -hex 32  (secret)
LIVEKIT_API_KEY=APIKeyChangeMe
LIVEKIT_API_SECRET=APISecretChangeMe

# --- TURN Domain ---
# Must match the TLS certificate and 'turn.domain' in livekit.yaml
TURN_DOMAIN=turn.example.com

# --- Port Overrides (optional) ---
# Change only if defaults conflict with your host
LIVEKIT_SIGNAL_PORT=7880
LIVEKIT_ICE_TCP_PORT=7881
LIVEKIT_TURN_TLS_PORT=5349
