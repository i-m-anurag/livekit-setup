# =============================================================================
# Production Environment Variables
# Copy this file to .env and fill in all values before deploying
# =============================================================================

# --- Domain Configuration ---
# Your main application domain (Angular + Express API)
APP_DOMAIN=app.example.com

# LiveKit signaling domain (WebSocket connections)
LIVEKIT_DOMAIN=livekit.example.com

# TURN server domain (must match TLS certificate)
TURN_DOMAIN=turn.example.com

# --- TLS / Let's Encrypt ---
CADDY_ACME_EMAIL=admin@example.com

# --- LiveKit API Credentials ---
# Generate with: openssl rand -hex 16 (for key) and openssl rand -hex 32 (for secret)
LIVEKIT_API_KEY=APIKeyChangeMe
LIVEKIT_API_SECRET=SecretChangeMe_Generate_With_openssl_rand_hex_32

# --- MongoDB ---
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=changeme_generate_strong_password

# --- JWT Secret for Express Auth ---
# Generate with: openssl rand -hex 64
JWT_SECRET=change_this_jwt_secret_in_production

# --- Redis (optional, if Redis requires authentication) ---
# REDIS_PASSWORD=your-redis-password
